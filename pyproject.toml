[project]
name = "etl-vps"
version = "0.1.0"
description = "ETL SAP → MySQL reescrito nível sênior"
requires-python = ">=3.12"
dependencies = [
    "pandas",
    "pydantic",
    "pydantic-settings",
    "python-dotenv",
    "mysql-connector-python",
    "openpyxl",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/etl_vps"]
